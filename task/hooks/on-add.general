#!/usr/bin/env python

import sys
import json
import os

added_task = json.loads(sys.stdin.readline())
print(json.dumps(added_task))

os.system(f"task export > ~/warehouse/taskwarrior/all-tasks.json")
sys.exit(0)
